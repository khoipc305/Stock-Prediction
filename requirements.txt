# Core data processing
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # Pin to NumPy 1.x for compatibility
pyarrow>=12.0.0
python-dateutil>=2.8.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Financial data
yfinance>=0.2.28

# Machine Learning
scikit-learn>=1.3.0
torch>=2.0.0
xgboost>=2.0.0
joblib>=1.3.0

# NLP and Sentiment Analysis
nltk>=3.8.0
transformers>=4.30.0
vaderSentiment>=3.3.2

# Reddit API (optional - not needed if using FinViz)
# praw>=7.7.0
# python-dotenv>=1.0.0

# Utilities
tqdm>=4.65.0
jupyter>=1.0.0
ipykernel>=6.25.0

# Interpretability & Visualization
# shap>=0.42.0  # Optional - DISABLED due to conflicts:
#   - shap 0.50+ requires NumPy 2.x (incompatible with other packages)
#   - shap <0.45 requires C++ build tools to compile
#   - Notebook 05 (SHAP) can be skipped or run with alternative interpretability methods
plotly>=5.14.0
kaleido>=0.2.1
